# Выбирашка

Веб-приложение для обучения дошкольников с системой авторизации, управлением заданиями и административной панелью.

Проект разработан на Python с использованием FastAPI и предназначен в качестве учебного проекта.

---

## Возможности

### Для пользователей

- Регистрация и авторизация
- Прохождение обучающих заданий
- Просмотр категорий
- Выполнение тестов
- Просмотр результатов

### Для администратора

- Управление пользователями
- Добавление и редактирование категорий
- Создание и изменение заданий
- Загрузка изображений
- Управление содержимым сайта

---

## Используемые технологии

### Backend

- Python 3.11+
- FastAPI
- SQLAlchemy
- Alembic
- Pydantic
- JWT (python-jose)
- Passlib (bcrypt)

### База данных

- PostgreSQL

### Frontend

- HTML
- CSS
- JavaScript

---

# Архитектура проекта

Проект построен по многослойной архитектуре.
backend/
│
├── api/            # REST API
├── models/         # ORM-модели SQLAlchemy
├── schemas/        # Pydantic-схемы
├── services/       # Бизнес-логика
├── database/       # Подключение к БД
├── migrations/     # Миграции Alembic
├── static/         # Статические файлы
├── templates/      # HTML-шаблоны
└── main.py         # Точка входа

### Назначение слоев

API

Принимает HTTP-запросы и возвращает ответы.

Models

Описывают структуру таблиц базы данных.

Schemas

Используются для проверки входящих данных и формирования ответов API.

Services

Содержат бизнес-логику приложения.

Database

Настройка подключения к PostgreSQL.

---

# Авторизация

В проекте используется JWT-аутентификация.

После успешного входа пользователь получает JWT-токен, который передается в заголовке:
Authorization: Bearer <token>

Сервер проверяет подпись токена и определяет пользователя.

---

# Запуск проекта

## 1. Клонировать репозиторий
git clone <repository_url>

cd preschoolers_training_site

---

## 2. Создать виртуальное окружение
python -m venv .venv

Активировать:

Windows
.venv\Scripts\activate

Linux/macOS
source .venv/bin/activate

---

## 3. Установить зависимости
pip install -r requirements.txt

---

## 4. Создать файл .env

Скопировать пример:
cp .env.example .env

или создать вручную.

---

## 5. Настроить PostgreSQL

Создать базу данных и указать параметры подключения в .env.

---

## 6. Выполнить миграции
alembic upgrade head

---

## 7. Запустить приложение
uvicorn backend.main:app --reload

---

После запуска приложение будет доступно по адресу
http://127.0.0.1:8000

Swagger-документация
http://127.0.0.1:8000/docs

---

# Структура базы данных

Основные сущности:

- Пользователи
- Категории
- Задания
- Ответы пользователей
- Результаты прохождения

---

# Тестовый администратор

При первом запуске приложения автоматически создается тестовый администратор (если он отсутствует в базе данных).

Данные администратора указываются в файле .env.

---
